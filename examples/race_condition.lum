entity Counter {
  count: Number
}

let c = Counter { count: 0 }
rule "Rule A" {
  every 1 s
  then update c.count to c.count + 1
}

rule "Rule B" {
  every 1 s
  then update c.count to c.count + 1
}
rule "Report" {
  every 1 s
  then show "Lumina Count: {c.count}"
}
